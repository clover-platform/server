spring:
  application:
    name: clover-main
  cloud:
    nacos:
      config:
        server-addr: https://nacos.zkit.org
        username: ${NACOS_USERNAME}
        password: ${NACOS_PASSWORD}
        file-extension: yaml
        refresh-enabled: true

# 基础包名
basePackage: plus.xyc.server.wiki
mapperPackage: src/main/resources/mapper
# 数据源
datasource:
  url: jdbc:mysql://mysql:7307/clover_wiki
# 激活的模块
actives:
  - catalog
# 模块配置
modules:
  - name: catalog
    tables:
      - catalog
